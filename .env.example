# WordPress GraphQL Endpoint (legacy - being migrated to Payload)
NEXT_PUBLIC_WP_URL=https://your-wordpress-site.com/graphql

# Payload CMS Configuration
PAYLOAD_SECRET=your-super-secret-key-here-change-in-production
DATABASE_URI=file:./payload.db
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3000

# Turso Database (for Vercel non-prod branches)
TURSO_DATABASE_URL=libsql://your-database.turso.io
TURSO_AUTH_TOKEN=your-turso-auth-token

# Admin Panel Password (legacy)
ADMIN_PASSWORD=your_secure_password
ADMIN_USERNAME=admin

# JSON Bin Configuration (legacy - migrating to Payload GridLayouts collection)
JSON_BIN_ID=your_jsonbin_id
JSON_BIN_API_KEY=your_jsonbin_api_key
JSON_BIN_BASE_URL=https://api.jsonbin.io/v3/b